class TimelineData {
  String id;
  String id_user;
  String username;
  String avatar;
  String content;
  String deskripsi;
  String kategori;
  String created_at;

  TimelineData(this.id, this.id_user, this.username, this.avatar, this.content, this.deskripsi, this.kategori, this.created_at);

}